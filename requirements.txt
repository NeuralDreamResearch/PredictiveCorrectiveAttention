torch>=2.4
transformers>=4.40
numpy>=1.24
# optional: fuses the Kalman-gain scan; the model falls back to a pure-PyTorch
# path automatically if triton is unavailable
triton>=3.0 ; platform_system == "Linux"
